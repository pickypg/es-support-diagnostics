linuxOS:
  top: "top -b -n1"
  netstat: netstat -an"
  ps: "ps -ef"
  top_threads: "top -b -n1 -H"

macOS:
  top: "top -l 1"
  netstat: "netstat -an"
  ps: "ps -ef"

winOS:
  ps: "tasklist /v"
  netstat: "netstat -ano"